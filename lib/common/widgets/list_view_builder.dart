import 'package:flutter/material.dart';

import './listview_header.dart';

class ListViewBuilder extends StatelessWidget {
  final ListviewHeader header;

  ListViewBuilder({
    @required this.header,
  });

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
